const { app, adminApp } = require('./firebaseConfig');

describe('Your Firebase Functions', () => {
  it('should do something', async () => {
    // Your test logic here using app and adminApp
  });
});
